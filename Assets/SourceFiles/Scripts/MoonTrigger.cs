using UnityEngine;

// Invisible clickable region over the moon. Clicking it several times in night
// mode triggers the day-runner easter egg (handled by GameManager).
public class MoonTrigger : MonoBehaviour
{
}
